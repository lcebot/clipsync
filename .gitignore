android/clipsync.properties
android/keystore.properties
*.jks
*.p12
*.pfx
*.keystore
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
# windows/clipsync.ini is deliberately NOT ignored: the repository carries a blank one, and the
# release zip is built from it. Being tracked, it is also the one file .gitignore cannot protect
# from your own edits — keep local values out of commits with
#     git update-index --skip-worktree windows/clipsync.ini
windows/clipsync.log
windows/install-state.json
windows/received/
__pycache__/
