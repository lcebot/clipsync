<?xml version="1.0" encoding="utf-8"?>
<!-- Material Symbols "add" (outlined, wght 400).
     No android:tint here, unlike ic_remove: this one is a MaterialButton icon, and the button's own
     iconTint (colorOnSurfaceVariant, via the outlined button's theme overlay) is what should colour
     it. A tint baked into the drawable would win over the button's state list. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FF000000"
        android:pathData="M19,13h-6v6h-2v-6H5v-2h6V5h2v6h6V13z" />
</vector>
